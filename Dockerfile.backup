# Dockerfile para el servicio de backup
FROM postgres:15-alpine

# Instalar cron y herramientas
RUN apk add --no-cache bash dcron

# Crear directorio de backups
RUN mkdir -p /backups

# Copiar scripts
COPY scripts/backup.sh /usr/local/bin/backup.sh
COPY scripts/restore.sh /usr/local/bin/restore.sh
RUN chmod +x /usr/local/bin/backup.sh /usr/local/bin/restore.sh

# Script de entrada que configura cron dinámicamente
COPY scripts/backup-entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]
